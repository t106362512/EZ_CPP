#include "stdafx.h"
#define PRODUCT(a,b) ((a)*(b)) // (a*b) return ?+a*b+?
using namespace std;

void _6_Define3() {

	int a = 3, b = 9;
	cout << "PRODUCT(a + 2, b + 3) = "<< PRODUCT(a + 2, b + 3);
	system("pause");
}